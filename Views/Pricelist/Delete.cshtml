@model Price_lists_CRM.Models.PricelistModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete @Html.DisplayFor(model => model.Title)?</h1>

<div class="row">
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <a asp-action="Index" class="btn btn-secondary me-1">Back to List</a>
        <input type="submit" value="Delete" class="btn btn-danger" />
    </form>
</div>

<h3>Are you sure you want to delete @Html.DisplayFor(model => model.Title)?</h3>
